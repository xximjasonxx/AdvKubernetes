apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-worker.fullname" . }}
  labels:
    {{- include "data-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-worker.selectorLabels" . | nindent 4 }}
