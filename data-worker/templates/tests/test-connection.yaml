apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-worker.fullname" . }}-test-connection"
  labels:
    {{- include "data-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
